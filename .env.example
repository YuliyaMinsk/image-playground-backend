# Server
HOST=0.0.0.0
PORT=8000

# Provider mode: stub | cheap | nano
IMAGE_PROVIDER_MODE=stub

# Daily generation limit
DAILY_LIMIT=100

# Prompt language mode: strict | warn | ignore
PROMPT_LANGUAGE_MODE=strict